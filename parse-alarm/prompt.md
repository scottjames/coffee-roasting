
write a python script to read and parse alarm (*.alrm) files from artisan scope coffee roasting software. parse the alarm file contents into a memory model. output the model in one of the following formats: markdown, json, text.  user will select the output format as command line argument.  example file is attached.


also add output format 'ascii' which is a space delimited ascii table used for nice formatting in terminal.  Also add pdf output format which prints a nice looking, very readable, 1-page format.

